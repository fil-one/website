# HubSpot service key or private app token with the `content` scope.
HUBSPOT_PRIVATE_APP_ACCESS_TOKEN=

# HubSpot blog group to read posts from. Optional — defaults to Fil One's
# production blog group; set it to point a preview/sandbox portal elsewhere.
HUBSPOT_BLOG_CONTENT_GROUP_ID=
